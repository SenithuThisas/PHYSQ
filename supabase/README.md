# Supabase

This folder will contain database migrations, seed data, and row-level security policies for PHYSQ.

Structure:
- `migrations/` – SQL files applied in order
- `seed/` – optional seed data
- `policies/` – RLS policies

